{
  "name": "spot-the-chibi",
  "version": "1.0.0",
  "description": "Spot The Chibi: 57-Symbol Matching Party Game for iOS and Web",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8080 --directory web",
    "cap:sync": "cap sync",
    "cap:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
